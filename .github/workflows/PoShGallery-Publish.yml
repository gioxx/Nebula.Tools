name: Publish to PowerShell Gallery

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate module manifest
        shell: pwsh
        run: |
          $psd1Path = Join-Path $PWD 'Nebula.Tools.psd1'
          Test-ModuleManifest -Path $psd1Path -Verbose

      - name: Prepare clean module package
        shell: pwsh
        run: |
          $outDir = "$PWD\build"
          if (Test-Path $outDir) { Remove-Item -Recurse -Force $outDir }
          New-Item -ItemType Directory -Path $outDir | Out-Null

          # Copy main files
          Get-ChildItem -Path $PWD -Include *.psd1,*.psm1,README.md,icon.png -File |
              Copy-Item -Destination $outDir

          # Copy modules folder if it exists
          if (Test-Path "$PWD\modules") {
              Copy-Item -Path "$PWD\modules" -Destination "$outDir\modules" -Recurse
          }

      - name: Publish module to PowerShell Gallery
        shell: pwsh
        run: |
          $modulePath = "$PWD\build"

          if (-not (Get-PSRepository -Name 'PSGallery' -ErrorAction SilentlyContinue)) {
              Register-PSRepository -Name 'PSGallery' -SourceLocation 'https://www.powershellgallery.com/api/v2' -InstallationPolicy Trusted
          }

          Publish-Module -Path $modulePath `
                         -NuGetApiKey "${{ secrets.NUGET_API_KEY }}" `
                         -Verbose
